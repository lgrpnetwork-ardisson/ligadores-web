<!DOCTYPE html>
<html lang="es">
<head><%- include('partials/head') %></head>
<body>
  <%- include('partials/nav') %>
  <main>
    <div class="container" style="text-align:center; padding-top:6rem;">
      <h1 style="color:var(--white); margin-bottom:1rem;"><%= title %></h1>
      <p style="color:var(--ink-dim);"><%= message %></p>
      <a href="/" class="btn btn-ghost" style="margin-top:2rem;">Volver al inicio</a>
    </div>
  </main>
</body>
</html>
